/**
 * Custom hook for accessing notification functionality
 * Re-exports the useNotification hook from the NotificationContext
 */

export { useNotification } from '../contexts/NotificationContext';
