/**
 * Re-export the comprehensive ErrorBoundary from common components
 * This maintains backward compatibility while using the enhanced error boundary
 */

export { ErrorBoundary as default } from '../common/ErrorBoundary';
